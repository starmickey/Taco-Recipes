<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <!-- Choice buttons -->
  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="0001" name="choice">🍗</button>
    <button type="submit" value="0002" name="choice">🥩</button>
    <button type="submit" value="0003" name="choice">🐟</button>
  </form>

  <!-- Recipe -->
  <div id="recipeContainer" class="hidden">
    <% if (!recipe) { %>
      <!-- If no recipe yet, show: -->
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } else { %>
      <!-- If recipe, show the recipe name-->
      <h2>
        <%= recipe.name %>
      </h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <% const { protein, salsa, toppings }=recipe.ingredients %>
        <li>
          <%= protein.name %>,
            <%= protein.preparation %>
        </li>
        <li>
          <%= salsa.name %>
        </li>
        <% toppings.forEach(topping=> { %>
          <li>
            <%= topping.quantity %>
              <%= topping.name %>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>

</html>